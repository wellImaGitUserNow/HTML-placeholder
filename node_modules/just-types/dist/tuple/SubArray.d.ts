import { ZeroTo100 } from '../constants';
import { Range } from '../number';
import { Slice } from './Slice';
export type SubArray<T extends any[] & {
    length: ZeroTo100;
}> = Slice<T, Range<0, T['length']>, Range<0, T['length']>>;
