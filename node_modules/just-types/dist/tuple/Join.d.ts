type Literal = string | number | bigint | symbol;
export type Join<T extends Literal[], Separator extends Literal> = T extends [infer Head, ...infer Tail] ? Tail extends [] ? `${Head}` : `${Head}${Separator}${Join<Tail, Separator>}` : '';
export {};
