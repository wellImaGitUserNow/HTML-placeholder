import { ZeroTo100 } from '../constants';
export type SubSequence<T extends any[] & {
    length: ZeroTo100;
}> = T extends [infer Head, ...infer Tail] ? [Head] | [Head, ...SubSequence<Tail>] | SubSequence<Tail> : [];
